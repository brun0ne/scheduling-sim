import Menu from './classes/Menu';

function main(){
    if (typeof document !== "undefined") {
        const menu = new Menu();
        menu.init();

        alert("Workin in progress! Currently not working. ");
    }
    else {
        
    }
}

main();
